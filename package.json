{
  "name": "verpkg",
  "version": "0.0.6",
  "main": "index.js",
  "repository": "https://github.com/subhranshudas/verpkg.git",
  "license": "MIT",
  "scripts": {
    "ci:version": "standard-version --branches main && git push --follow-tags origin main",
    "ci:version:beta": "standard-version --branches main --prerelease beta && git push --follow-tags origin develop",
    "ci:publish": "npm publish --access public"
  },
  "devDependencies": {
    "standard-version": "^9.3.2"
  }
}
